<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Notes</title>
    <link rel="stylesheet" href="/public/assets/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto+Slab&display=swap" rel="stylesheet">
</head>
<body>
    <h1 id="editHeader">Edit <%= note.title %></h1>
    <div class="editNotePage">
        <form action="/notes/<%= note._id %>?_method=PUT" method="POST">
            <input type="text" name="note[title]" value="<%= note.title %>" required>
            <textarea name="note[content]" id="contTextArea" cols="30" rows="10" required><%= note.content %></textarea>
            <button type="submit">EDIT</button>
        </form>
    </div>
    <a id="edtBackBtn" href="/notes/<%= note._id %>">Back</a>
</body>
</html>